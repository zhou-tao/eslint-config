const a = 'a'

const obj = {
  'a-1': 1,
  'b': 2
}

const arr = []

function add(a, b) {
  console.log(a + b)
}

switch (a) {
  case 'a':
    break

  default:
    break
}

const value = arr?.length

console.log(a, obj, add, arr)
