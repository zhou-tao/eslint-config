{
  "name": "@tooz/eslint-config-monorepo",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "lint": "pnpm -r run eslint .",
    "test": "pnpm -r run test",
    "build": "pnpm -r run build",
    "prepare": "pnpm -r run stub",
    "release": "bumpp -r && pnpm -r publish"
  },
  "author": "tooz",
  "license": "MIT",
  "devDependencies": {
    "@tooz/eslint-config": "workspace:*",
    "bumpp": "^9.1.1",
    "eslint": "^8.44.0"
  }
}
